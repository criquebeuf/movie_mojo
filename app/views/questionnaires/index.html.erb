<div class="container mt-3">
  <h1>Questionnaires Index (testing)</h1>
  <%= link_to "Start", questionnaires_path, data: { turbo_method: :post }, class: "btn btn-primary" %>
  <h2 class="mt-3">My tests:</h2>
  <% @questionnaires.each do |questionnaire|  %>
    <div>
      <%= link_to questionnaire.title, questionnaire_path(questionnaire) %>
    </div>
  <% end %>
</div>
